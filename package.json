{
  "name": "cv",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "prepublish": "npm run build",
    "build": "npm run build:html && npm run build:scss",
    "start": "nodemon src/server.js",
    "filesize": "./scripts/filesize.js",
    "compile:scss": "sass src/main.scss src/main.css -I ./node_modules -c --no-source-map && npm run filesize -- src/main.css",
    "minify:scss": "postcss dist/main.css -o dist/main.css && npm run filesize -- dist/main.css",
    "build:scss": "npm run compile:scss && npm run minify:scss",
    "build:html": "html-minifier src/index.html -o dist/index.html --collapse-whitespace --remove-comments --remove-optional-tags --remove-redundant-attributes --remove-script-type-attributes --remove-tag-whitespace --use-short-doctypei && npm run filesize -- dist/index.html",
    "test-stylelint": "stylelint src/main.scss",
    "version:alpha": "npm version prerelease --preid=alpha",
    "version:beta": "npm version prerelease --preid=beta"
  },
  "author": "Adrian V",
  "license": "MIT",
  "devDependencies": {
    "@fwrlines/swatch": "^1.0.0-beta.5",
    "css-mqpacker": "^7.0.0",
    "cssnano": "^4.1.10",
    "filesize": "^6.1.0",
    "html-minifier": "^4.0.0",
    "nodemon": "^2.0.6",
    "postcss-cli": "^8.2.0",
    "postcss-preset-env": "^6.7.0",
    "postcss-scss": "^3.0.4",
    "sass": "^1.29.0",
    "stylelint": "^13.7.2",
    "stylelint-config-palantir": "^5.0.0",
    "stylelint-scss": "^3.18.0"
  }
}
